<%= link_to "Home", root_path %>

<h1>Brands</h1>

<ul>
    <% @brands.each do |brand| %>
        <li>
            <%= link_to brand.name, brand %>
        </li>
    <% end %>
</ul>

<%= link_to "New Brand", new_brand_path %>